# Short Response — LG 7.0: Responsive Units

Answer each question in 2–3 sentences. 

---

## Question 1 — Describe

What is the difference between a static unit like `px` and a responsive unit like `%` or `vh`?

Describe what makes a unit responsive and why that matters when building a website.
the difference between px and %/vh is that px stay the same it doesnt change the size if the screen gets big or small, % or vh it doesnt change the size wheather the screen is big or small. the responsive unit matter because if you are building a website you want the veiwers to see the words and know what is happening px doesnt change the size if the screen size is small or large as it will make it hard to read, so it is important to use responsive units as it is better then px.



---

## Question 2 — Explain

Look at these two CSS rules:

```css
.image {
  width: 400px;
}

.image {
  width: 50%;
}
```

Explain what happens to the image on a small screen with each rule. Why does one behave better than the other?
on the small screen for px it stays the same maintaining its big letters but when the screen size change then it changes as well, meanwhile for the % it changes its size but still maintains its big letters but when it changes it screen size the letter stay the same not becoming smaller. the % on is better than the px one because if you were working on your phone the px version would be small and hard to read but the % one is easier as the screen size dont affect the letter size.




